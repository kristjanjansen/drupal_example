<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <link href="https://fonts.googleapis.com/css?family=Muli:400,800" rel="stylesheet">
        <style>
            * {
                margin: 0;
            }
            body {
                font-family: Muli, sans-serif;
                font-size: 1.1em;
            }
            header {
                padding: 6rem 3rem 2rem 3rem;
                background: #054a6f;
            }
            main {
                margin: 3rem;
                max-width: 800px;
            }
            aside {
                font-size: 0.95rem;
            }
            p {
                line-height: 1.75rem;
                color: rgba(0,0,0,0.7);
                margin-bottom: 1em;
            }
            p:last-child {
                margin-bottom: 2em;
            }
            a {
                text-decoration: none;
                color: rgba(0,0,0,0.9);
            }
            h1 {
                color: rgb(255,255,255);
                font-size:2.5em;
                text-shadow: 1px 1px 0px rgba(0,0,0,0.35);
            }
            h2 {
                color: rgba(0,0,0,0.65);
                margin-bottom: 0.75rem;
            }
            h3 {
                color: rgba(0,0,0,0.65);
                margin: 3rem 0 1rem 0;
            }
            h4 {
                color: rgba(0,0,0,0.35);
                font-weight: normal;
                margin-bottom: 0.75rem;
            }
            h4 a {
                color: rgba(0,0,0,0.75);
            }
            img {
                float: right;
                padding-left: 1rem;
            }
            pre {
                color: rgba(0,0,0,0.7);
                line-height: 1.5em;
            }
        </style>
    </head>
    <body>
        <div id="app"></div>
        <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
        <script src="https://unpkg.com/jsonapi-parse"></script>
        <script src="https://unpkg.com/moment"></script>
        <script src="/themes/custom/example_js2/src/main.js" type="module"></script>
    </body>
</html>
